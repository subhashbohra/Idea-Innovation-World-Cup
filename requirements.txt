# dbFlow Demo — Python dependencies
# Install with: pip install -r requirements.txt

fastapi==0.115.0
uvicorn[standard]==0.32.0
httpx==0.27.0
pydantic==2.9.0
pydantic-settings==2.6.0

# Vector store / embeddings
sentence-transformers==3.3.0   # for semantic retrieval (CPU-only, ~84MB model)

# Optional: pgvector for production
# asyncpg==0.30.0
# pgvector==0.3.6

# Optional: Trino connector for real Iceberg queries
# trino==0.330.0
